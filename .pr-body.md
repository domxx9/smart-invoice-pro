## Summary

- **SMA-277 fix**: Remove hardcoded `localhost:3100` fallback from `feedbackSubmit.js` — env var must be set or request silently skipped (no fallback)
- **SMA-283 fix**: `platformFetch.js:19` — truncate API error messages to 200 chars via `.slice(0, 200)` on `JSON.stringify(res.data)` to prevent log injection

## What changed

- `src/utils/feedbackSubmit.js`: removed module-level `PAPERCLIP_URL` constant with `|| 'http://localhost:3100'` fallback; URL is now read inside the function body (already guarded by early return when env var absent)
- `src/api/platformFetch.js:19`: error throw now uses `.slice(0, 200)` on `JSON.stringify(res.data)`
- `src/api/shopify.js` and `src/api/squarespace.js`: no changes (SMA-283 fix supersedes the old approach that was in these files)

## Test plan

- [x] `npm test` — all tests pass
- [x] No `localhost:3100` fallback in `feedbackSubmit.js`
- [x] `platformFetch.js` error uses `.slice(0, 200)`
- [x] CI green (lint, typecheck, build)
